language: python
python:
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"
install:
  - pip install . && pip install -r test_requirements.txt
script:
  - ./linters.sh && pytest --cov=usfm_references tests/
after_success:
  - coveralls
